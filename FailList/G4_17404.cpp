#include <bits/stdc++.h>
using namespace std;
int dp[1000][3];//r g b
int rgb[1000][3];
int main()
{
    int n;
    cin >> n;
    for (int i = 0 ; i < n ; ++i)
    {
        for (int j = 0 ; j < 3 ; ++j)
            cin >> rgb[i][j];
    }
    for (int i = 2 ; i < n ; ++i)
    {
        if (i == 0)
        {

        }
    }
}